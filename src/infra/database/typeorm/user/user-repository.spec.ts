describe('User Repository MongoDB', () => {
  test('should return an user when create on DB success', () => {
    /*
    const userRepository = new UserRepository()

    const createUserParams = {}

    const user = userRepository.create()
    */
  })
})
